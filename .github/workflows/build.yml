name: Build
on: [push, pull_request]
jobs:
  build: 
    uses: IngSis-Grupo1-2024/snippet-infra/.github/workflows/build.yml@main
    with:
      actor: ${{ env.ACTOR }}
    secrets:
      token: ${{ env.TOKEN }}
